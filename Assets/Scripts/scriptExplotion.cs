using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class scriptExplotion : MonoBehaviour {
 
	// Update is called once per frame
	void Update () {
		Destroy (gameObject, 1.1f);
	}
}
